charts:
  # not minikube friendly
  # - name: nginx-ingress
  #  version: 0.28.2
  - name: jenkins-casc
    discriminator: one
    version: 0.1.0
    #
    # local chart dir style
    #
    # repo: ../helm-jenkins-casc
    #
    # github-pages style
    #
    # repo: https://the-watchmen.github.io/helm-charts/
    #
    # github raw style
    #
    repo: https://raw.githubusercontent.com/the-watchmen/helm-charts/helm-repo/
    # wait: true
  #   secrets:
  #     - key: secrets.GHE_HEH_SID_TOKEN
  # - name: jenkins
  #   version: 0.18.0
  #   secrets:
  #     - key: Master.AdminPassword
  - name: jenkins-casc
    discriminator: two
    version: 0.1.0
    repo: https://raw.githubusercontent.com/the-watchmen/helm-charts/helm-repo/
